{
  "name": "data-labelling-server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "migrate:test": "npx prisma migrate dev --name test",
    "docker:up": "docker-compose -f tests/docker-compose.yml up -d",
    "docker:down": "docker-compose -f tests/docker-compose.yml down",
    "test": "npm run docker:up && npm run migrate:test && jest -i --no-cache  --detectOpenHandles --runInBand --forceExit",
    "test:dev": "jest --no-cache  --detectOpenHandles --runInBand --forceExit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^2.29.1",
    "apollo-server-express": "^3.1.2",
    "argon2": "^0.28.2",
    "express": "^4.17.1",
    "googleapis": "^84.0.0",
    "graphql": "^15.5.1",
    "graphql-upload": "^12.0.0",
    "jsonwebtoken": "^8.5.1",
    "nodemon": "^2.0.12"
  },
  "devDependencies": {
    "apollo-server": "^3.1.2",
    "apollo-server-integration-testing": "^3.0.0",
    "dotenv": "^10.0.0",
    "jest": "^27.0.6"
  }
}
